#include<iostream>
using namespace std;
int main()
{
    int i,n,c,d;
    cin>>n;
    int a[n];
    for(i=0;i<n;i++)
    {
        cin>>a[i];
        if(a[i]%2==0)
        c++;
        else
        d++;
    }
    cout<<"Odd: "<<d<<endl<<"Even: "<<c;
}
